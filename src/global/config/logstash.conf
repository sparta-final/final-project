input {
    jdbc {
        jdbc_driver_library => "/usr/share/logstash/mysql-connector-java-8.0.28.jar"
        jdbc_driver_class => "com.mysql.cj.jdbc.Driver"
        jdbc_connection_string => "jdbc:mysql://database-1.cztc0r3bkhqe.ap-northeast-2.rds.amazonaws.com:3306/sixpack"
        jdbc_user => "root"
        jdbc_password => "Guswl1219"
        schedule => "* * * * *"
        tracking_column => "updated_at"
        statement => "select * from gym left join gym_img as gymImg on gym.id = gymImg.gym_id 
        where gym.updated_at > :sql_last_value order by gym.updated_at asc"
        use_column_value => true
    }
}


output {
    elasticsearch {
        hosts => "elasticsearch:9200"
        index => "gym"
        document_id => "%{gym_id}"
    }
    stdout { codec => rubydebug }
}